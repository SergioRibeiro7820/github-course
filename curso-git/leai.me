Aula 26 Git Stash.

Comando usado para guardar um arquivo e mexer em outro sem precisar subir esse arquivo.

comandos:

git stash - cria um arquivo (wip)

git stash apply - retorna ao arquivo que estava trabalhando

git stash list - lista os stash que está trabalhando

git stash clear - limpa os stash que está trabalhando, sem modificações guardadas
rsrsrsrs
